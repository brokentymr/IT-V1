# Session handoff — 2026-07-01 04:00:48 UTC

Where we are, to pick up in the morning.

## State
- Branch `architecture`, in sync with origin. HEAD `fbe00cb`.
- App LIVE at markets.kuramoto.io (Caddy basic-auth, user `operator`). 94 tests pass, `tsc` clean.
- 5 daily/weekly timers live: monitor 09:00 · forward 10:30 · filings 11:00 · prices 22:00 · sentiment Sun 12:00.

## Shipped this session
- **Phases 5.5 → 8 complete** (Areas of Interest, Calendar+scheduler+price-anomaly, Brand/Sentiment,
  and the content spider: deck / newsletter / short-form / podcast studio + plain-language layer).
- **Owner feedback pivot → consumer reframe** ("stop showing your work"). Diagnosed + designed via a
  6-agent workflow, then:
  - **Intake bug FIXED** (`d55fdcc`) — Aritzia/Reformation no longer substitute an unrelated US filer;
    they land as themselves. Name-agreement gate before any ticker "confirms."
  - **Consumer front-end + auto-on-add SHIPPED** (`fbe00cb`):
    - `/` = swipe-first card feed; `/c/[id]` = live swipe deck; operator console demoted to "admin".
    - Human status vocab (Building clarity / Ready for your review / Live).
    - Adding an asset auto-runs the whole pipeline (`JOB.ONBOARD_ASSET`), stopping at the §8 checkpoint.

## Next (the level-up roadmap, in order)
1. **C — Confidence-deepening loops** (recommended next): low confidence / unverified claims auto-escalate
   scrutiny (source-gap fill → re-lens → bump model tier → re-verify) until a real bar or budget.
   Touches `lib/engines/research.ts` + the coverage pass; new `lib/config/desk.ts`.
2. **D — Dialogue engine**: replace the podcast segment-outline with a real ~30-min organic two-host
   conversation (beat-sheet → per-beat dialogue expansion; kills the truncation gotcha). `lib/engines/podcast.ts`.
3. Still pending from before the pivot: **Phase 9** (Sector Cohort engine + hardening/backups/monitoring/
   spend-alerts + compliance consult before publishing at scale).

Owner leaning TBD between C-first (quality lifts everything) and D-first (the podcast is the exciting part).

## Notes / gotchas
- Content generators run sequentially (~4-5 min for the full spider); the in-app button runs inline.
- Long audience prose in one JSON field can truncate — ceilings raised + lengths bounded; D's beat-sheet
  approach fixes this structurally.
- Auto-on-add fires the Opus desk per add against the $50/mo ceiling (the shared client self-guards).
- Full detail in the memory files (investing-together-project, level-up-roadmap, content-vision-voice).
